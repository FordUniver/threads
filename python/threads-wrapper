#!/usr/bin/env bash
# Wrapper to run Python threads module without changing directory
# Uses --project to find pyproject.toml without changing CWD
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec uv run --quiet --project "$SCRIPT_DIR" python -m threads "$@"
