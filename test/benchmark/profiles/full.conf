# Full benchmark profile
# ~30 minutes runtime

# Categories to run
CATEGORIES="startup query mutation scale"

# Iterations per benchmark
ITERATIONS=100

# Warmup runs
WARMUP=3

# Thread counts for query benchmarks
QUERY_THREADS=200

# Scale points (comprehensive)
SCALE_POINTS="10 50 100 200 500 1000"

# Mutation iterations (lower due to state management)
MUTATION_ITERATIONS=50

# Include all tests
SKIP_MUTATION=0
