# Quick benchmark profile
# ~5 minutes runtime

# Categories to run
CATEGORIES="startup query"

# Iterations per benchmark
ITERATIONS=20

# Warmup runs
WARMUP=2

# Thread counts for query benchmarks
QUERY_THREADS=50

# Scale points (subset)
SCALE_POINTS="50 200"

# Skip mutation benchmarks in quick mode
SKIP_MUTATION=1
